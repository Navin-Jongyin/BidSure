import 'package:flutter/material.dart';

class MyFollowTile extends StatelessWidget {
  const MyFollowTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
